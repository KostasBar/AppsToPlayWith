import { Routes } from '@angular/router';
import { MarkdownEditorComponent } from './markdown-editor/markdown-editor.component';

export const routes: Routes = [
  { path: '', component: MarkdownEditorComponent }, // Default route
];
